import React from "react";
import { ConfirmModalTemplate } from "./ConfirmModal.template";

const ConfirmModal = (props) => {
  return <ConfirmModalTemplate {...props} />;
};

export { ConfirmModal };
